{
  "name": "derrickshowersCom",
  "version": "0.0.1",
  "description": "my portfolio site",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --style compressed static/css/scss/main.scss:static/css/style.css",
    "sass-watch": "sass --watch static/css/scss/main.scss:static/css/style.css",
    "build": "npm run sass",
    "watch": "npm run sass-watch"
  },
  "author": "Derrick Showers",
  "dependencies": {
    "express": "~3.5.1",
    "express3-handlebars": "~0.5.0"
  }
}
